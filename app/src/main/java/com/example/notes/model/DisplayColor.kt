package com.example.notes.model

class DisplayColor {
    var darkColor: Int
    var lightColor: Int
    var textColor: String

    constructor(darkColor: Int, lightColor: Int, textColor: String) {
        this.darkColor = darkColor
        this.lightColor = lightColor
        this.textColor = textColor
    }
}