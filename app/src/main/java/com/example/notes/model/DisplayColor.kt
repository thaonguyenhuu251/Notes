package com.example.notes.model

class DisplayColor {
    var darkColor:Int
    var lightColor:Int
    var textColor:Int

    constructor(darkColor: Int, lightColor: Int, textColor: Int) {
        this.darkColor = darkColor
        this.lightColor = lightColor
        this.textColor = textColor
    }
}